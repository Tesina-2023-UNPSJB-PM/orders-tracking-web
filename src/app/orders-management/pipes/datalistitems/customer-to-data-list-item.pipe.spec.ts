import { CustomerToDataListItemPipe } from './customer-to-data-list-item.pipe';

describe('CustomerToDataListItemPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomerToDataListItemPipe();
    expect(pipe).toBeTruthy();
  });
});
