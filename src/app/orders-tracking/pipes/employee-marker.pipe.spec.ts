import { EmployeeMarkerPipe } from './employee-marker.pipe';

describe('EmployeeMarkerPipe', () => {
  it('create an instance', () => {
    const pipe = new EmployeeMarkerPipe();
    expect(pipe).toBeTruthy();
  });
});
